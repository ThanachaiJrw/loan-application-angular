// features/loan/loan.routes.ts
import { Routes } from '@angular/router';
import { LoginComponent } from './login';

export const LoginRoutes: Routes = [
  { path: 'login', component: LoginComponent },
];
